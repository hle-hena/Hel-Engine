# Hel Engine

Hel Engine is a simple rendering engine made by hle-hena.

## STATUS - UPDATE

`hle-hena`: I am restarting everything to better understand the engine. Therefore, the engine will not be here for a while.

## INSTALL

To install in release mode: ```cmake -S . -B build && cmake --build build -- -j```
To install in debug mode: ```cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build -- -j```